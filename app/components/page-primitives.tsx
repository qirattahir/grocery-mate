import Link from "next/link";

export function PageHeader({ eyebrow, title, description, action }: { eyebrow?: string; title: string; description: string; action?: { href: string; label: string } }) { return <header className="page-header"><div>{eyebrow && <p className="eyebrow">{eyebrow}</p>}<h1>{title}</h1><p className="page-description">{description}</p></div>{action && <Link className="button button-primary header-action" href={action.href}><span aria-hidden="true">＋</span>{action.label}</Link>}</header>; }
export function SectionHeading({ title, link }: { title: string; link?: { href: string; label: string } }) { return <div className="section-heading"><h2>{title}</h2>{link && <Link href={link.href}>{link.label}<span aria-hidden="true"> →</span></Link>}</div>; }
export function EmptyState({ icon, title, description, action }: { icon: string; title: string; description: string; action?: { href: string; label: string } }) { return <div className="empty-state"><span className="empty-icon" aria-hidden="true">{icon}</span><h2>{title}</h2><p>{description}</p>{action && <Link className="button button-secondary" href={action.href}>{action.label}</Link>}</div>; }
export function StatusDot({ children, tone = "mint" }: { children: React.ReactNode; tone?: "mint" | "gold" | "coral" }) { return <span className={`status status-${tone}`}><span className="status-dot" aria-hidden="true" />{children}</span>; }
